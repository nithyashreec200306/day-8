#include <stdio.h>
#include <math.h>
int add()
{
    int x,y;
    scanf("%d%d",&x,&y);
    int sum=x+y;//hera we can use * this operator also with the declaration in tha name of sum
    int a=50;
    //return x+y
}
    int main()
    {
        int x=add();
        printf("%d",x);
        printf("%d",add());
        
    }